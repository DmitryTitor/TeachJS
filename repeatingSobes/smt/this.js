const f = (a, b) => a + b;
const obj = {
  a: 1,
  b: 2,
  c: 3,
};

// возвращает ключи
Object.keys
// возвращает значения
Object.values
// возвращает массив - ключ-значения
Object.entries


  // function fff() {
  //   const arr2 = {};

  //   Object.entries(obj).forEach(([key, value]) => {
  //     arr2[value] = key;
  //   });  

  //   console.log(arr2);

  //   return arr2;
  // }

  // fff();



  // f(...arr);

  // const f2 = (...args) => args.reduce((prev, cur) => prev + cur, 11);

  // console.log(f2(1, 2, 3));


  .then
  .then
  .then
  .catch
  .then
  .then
  .then
  .catch
  .finally

const arr3 = [1, 2, 3, 4];

const [a, ...arr] = arr3



